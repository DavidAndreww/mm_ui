import React from 'react';

const queryEngineStyle = {
  
}

export const QueryEngine = () => {
  return (
    <div id='query-engine' className='component-boundary' style={queryEngineStyle}>
      QueryEngine
    </div>
  )
};